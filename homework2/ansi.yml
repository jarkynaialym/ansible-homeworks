- hosts: all
  tasks: 
  - name: install apache
    include: apache.yml
  - name: install packages
    include: packages.yml  
  - name:   install php
    include: php.yml
  - name: Install wordpress
    include: wordpress.yml  
